# Core dependencies for Claude CLI Launcher
anthropic>=0.25.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Interactive CLI interface
inquirerpy>=0.3.4
prompt_toolkit>=3.0.36

# Optional rich formatting (with graceful fallback)
rich>=13.0.0

# Event loop compatibility (optional but recommended)
nest_asyncio>=1.5.0

# Development and testing dependencies
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Code quality
ruff>=0.1.0
mypy>=1.0.0
bandit>=1.7.0

# Type stubs
types-requests>=2.31.0